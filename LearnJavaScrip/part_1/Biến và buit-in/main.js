//alert ('hi Javascip !!!') /*Alert hiển thị thông báo */ 

/*
Cách Khai báo Biến:
    *** Lưu ý : - Tiên biến phân biệt chữ hoa và chữ thường
                - Ký tự đầu tiên phải là Chữ
                - Nếu không có ; thì không được viết lên cùng 1 dòng

    Cấu trúc : var <Name> = 'Giá trị được gán';
*/

var fullName = 'Vu Van Thang';
var age='23';


/*---------BUILD-IN ---- */
/** 
 - Build-in là thuật ngữ những hàm đã được xây dựng sẵn
 - có 6 hàm được xây dựng sẵn của js
    1. Alert : hàm hiển thị thông báo

    2. Console : truy cập các hàm:
        menthod :
            + Console.log ( ) : in ra nội dung(biến, số , chuỗi kí tự..vv..) ở tab console bên trình duyệt.
            + Console.warn( ) : in ra cảnh báo //                                               .
            + Console.error( ) : in ra lỗi    //

    3. Confirm : hiển thị thông báo xác nhận
        prototype : confirm ('');
    
    4. Prompt : hiển thị thông báo có input 
        prototype : prompt ('');
    
    5. setTimeout : 
        prototype : setTimeout( Funtion() { }, tham số <time>);
        - tham số <time> : thời gian (miliseconds) để funtion xuất hiện
    
    6. setInterval :
        prototype : setInterval( Funtion() { }, tham số <time>);
        - tham số <time> : cách 1 khoảng thời gian sẽ hiển thị 1 lần.
*/

