ARRAY METHOD

1. forEarch() : duyệt qua từng phần tử của mảng

    courses.forEach(<callback> (<element>, index){
        //callback
    });

2. every() : kiểm tra tất cả phần tử của mảng, phải tuân thủ 1 điều kiện nào đó
    ** trả về kiểu boolean 

    courses.every(<callback> (<element>, index){
        //callback
    });

3. some() : Ngược lại với every

4. find() : Tìm kiếm 

5.filter()

6***. map() : chỉnh sửa , thay đổi element trong array

    var newVariable = variable.map(<just function> (<đối số>, index))
    // xem lại để hiểu hơn
    // dùng để hiển thị trang web
7. reduce()

    reduce(<1 đối số có thể là 1 function>(<biến lưu trữ>,<giá trị hiện tại***>,<chỉ mục###>, <mảng gốc>){} , <khởi tạo giá trị ban đầu cho biến lưu trữ>)
        *: THƯỜNG THÌ FUNCTION sẽ có ít nhất 2 giá trị là biến lưu trữ và giá trị hiện tại.
    Ex : tính tổng coi reduce
    courses.reduce(function(totalCoin, ){//code} , 0) ;

    Xem lại video (RẤT KHÓ)

    *** : mỗi khi duyệt đến 1 phần tử, thì giá trị hiện tại đang nằm ở vị trí đó
    ### : chỉ mục của giá trị hiện tại